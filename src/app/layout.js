import { Inter } from "next/font/google"
import { ChakraProvider, Flex, Text } from "@chakra-ui/react"
import Link from "next/link"

const inter = Inter({ subsets: ["latin"] })

export const metadata = {
  title: {
    default: `Carlo's First NextJS Website`,
    template: `%s | Carlo's website`,
  },
  description: "Generated by create next app",
}

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body className={inter.className}>
        <ChakraProvider>
          <header>
            <Flex bgColor={"blue.100"} gap={6} justifyContent={'center'} alignItems={'center'} >
              <Link href={'/'}><Text>Home</Text></Link>
              <Link href={'/complex-dashboard'}><Text>Dashboard</Text></Link>
              <Link href={'/product'}><Text>Product</Text></Link>
              <Link href={'/login'}><Text>Login</Text></Link>
              <Link href={'/register'}><Text>Register</Text></Link>
              <Link href={'/forgot-password'}><Text>Forgot Password</Text></Link>
              <Link href={'/gallery'}><Text>Gallery</Text></Link>
            </Flex>
          </header>
          {children}
          <footer>
            <Flex bgColor={"green.100"}>
              <Text>Footer section</Text>
            </Flex>
          </footer>
        </ChakraProvider>
      </body>
    </html>
  )
}
